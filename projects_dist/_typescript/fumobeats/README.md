Light Vite + typescript ecosystem for HTML 5 game development.

Includes a bunch of generic functions from my own lib in the `/src` folder.

NOTE: vite-array-buffer required to load binary assets.

Features:
- Import aliases for every folder inside src
- Typescript hot module replacement
- Bundling
- Presetted project metadata